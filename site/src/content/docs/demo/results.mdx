---
title: Record a run
description: Seal inputs, record outputs, and refuse a confirmatory claim that postdates the outcomes
tableOfContents: false
---

This notebook demonstrates `results` — [see documentation](/reproducible-science/tools/results/).

1. Seals the inputs, hashing them before the run rather than after.
2. Records what the run produced, and binds a manuscript claim to it.
3. Refuses a second run recorded under an id that already exists.
4. Records that the outcomes were seen.
5. Refuses to call a later run confirmatory, and reports the ordering when forced anyway.

<div class="jlite">
  <iframe
    src={`${import.meta.env.BASE_URL.replace(/\/$/, "")}/jlite/lab/index.html?path=results.ipynb`}
    title="Record a run, in JupyterLite"
    loading="lazy"
  ></iframe>
</div>

## Not possible in WebAssembly

`results` needs neither git nor a subprocess, so nothing here stands in for anything. Every
command in this notebook is the one you would run locally.
