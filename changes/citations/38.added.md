**[citations] `citations projects` reports which project names nothing answers to, and `citations tags` groups records against a declared vocabulary.** Ninety records named `evaluation-scope` hours after that repository was renamed and nothing reported it, because `cited_by` is a free-text key no check compared against the projects that exist. Tags are declared on a paper in `papers.yaml` and reach records through `cited_by`, so a work a paper cites tomorrow carries them without anything being re-run.
