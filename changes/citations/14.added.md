`verify` reads a PDF with pdfplumber or pypdf where poppler is absent or fails, so
`pip install citations` can check a PDF with no system package. Every result names the reader
that produced the text and, where it was not the preferred one, why. `--triangulate` asks
every installed reader and reports disagreement as `indeterminate`, a fourth outcome that
does not assert the passage is missing.
