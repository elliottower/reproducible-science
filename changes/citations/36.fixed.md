**[citations] A `.tex` source is read with `detex` rather than `pdftotext`.** A `.tex` with no declared `extract_cmd` got the PDF reader, which answered `Syntax Error: Couldn't read xref table`, and every quotation in it graded `unchecked` until an author declared `detex` by hand — for the commonest manuscript format in this domain, with `detex` already among the default extractors.
