# Changelog

Release notes are per package: each releases on its own tag, so each has its own history
below. Entries are assembled from fragments under `changes/`.

<!-- towncrier release notes start -->

## Unreleased

Nothing released from the monorepo layout yet. Before the first release from here, each
package's PyPI trusted publisher must be repointed at this repository and its per-package
workflow, and leaf packages must be released before `reproducible-science`, which declares
version floors on the other three.
